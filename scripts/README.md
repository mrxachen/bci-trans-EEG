# Scripts

This directory will hold non-interactive task entrypoints such as:
- dataset registration helpers
- preprocessing launchers
- benchmark runners
- result collation commands

Keep scripts thin and delegate logic to the package under `src/bci_trans_eeg/`.
