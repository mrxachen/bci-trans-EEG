# Configs

This directory is reserved for project-level and experiment-level configuration files.

Planned usage:
- dataset registry and dataset-combination definitions
- preprocessing presets
- protocol definitions
- baseline and method configurations
- training and evaluation settings

Keep this directory config-driven rather than code-driven as the implementation starts.
